# This file makes the services directory a Python package
# Keep it EMPTY to avoid circular imports
# The service_registry will handle all imports dynamically

# DO NOT import services here - it causes circular import issues!
